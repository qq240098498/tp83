# tp83 依赖台账与版本核查平台

一个本地小工具：把各个项目用到的依赖集中登记起来。页面上先建项目，再往项目下面登记依赖名称、版本、许可、责任人与状态；同一个依赖被两个项目用到时，版本高低一眼能比出来。依赖名写得不合规矩、版本不是三段数字、同一个项目里重复登记同一个依赖，保存时当场就会指出来。

依赖区下面还有一块许可清单：维护允许使用与一律不允许使用两类许可，核查时按项目与依赖逐条给出结论——落在允许清单里的通过、落在不允许清单里的不通过、两边都没写到的按待确认列出。许可写法的大小写、空格差异与常见别名都会先归一，再对照清单。

数据全部存在本机的数据文件里，不连数据库，也不调用任何外部服务。

## 技术栈

- 后端：Node.js（>=18）与 Express，前后端同一个进程
- 前端：原生 HTML、CSS 与 JavaScript，页面文件由后端静态目录直接提供，没有构建步骤
- 存储：本地 JSON 文件 `data/db.json`

## 项目结构

```
tp83/
├── server/
│   ├── index.js        # 服务入口、静态目录、接口路由
│   ├── api.js          # 项目、依赖登记与许可清单的对外动作
│   ├── projects.js     # 项目的校验、新增、修改与删除
│   ├── deps.js         # 依赖登记的校验、新增、修改、查询与删除
│   ├── licenses.js     # 许可清单的维护与逐条核查
│   ├── licenseNames.js # 许可写法的归一口径与常见别名表
│   ├── errors.js       # 带错误码与出错位置的业务异常
│   └── store.js        # 数据读写：载入、整理结构与落盘
├── public/
│   ├── index.html      # 主界面：项目区、依赖登记区、许可清单区与登记表单
│   ├── app.js          # 页面交互逻辑
│   └── style.css       # 页面样式
├── data/
│   └── db.json         # 本地数据文件（含三个项目、十八条依赖登记与一份许可清单）
├── package.json
├── .gitignore
└── README.md
```

## 启动方式

```bash
npm install
npm start
```

启动后打开页面地址：<http://localhost:5083>

端口固定为 `5083`，如需临时改动可在启动前设置环境变量 `PORT`。

## 页面怎么用

1. 顶栏右侧可以填当前操作者，填过之后浏览器会记住，新建登记时会把它带进责任人
2. 项目区列出项目名称、负责人、说明与这个项目下已经登记了多少条依赖；可以新增项目，也可以改名、换负责人、删除
3. 依赖登记区顶部可以按项目、状态、许可筛选，再按依赖名、责任人或者备注搜索，右上角刷新按钮重新拉取一次
4. 点新建登记，选好所属项目，填上依赖名称、版本，按需要补许可、责任人、状态与备注；列表里点编辑会把这条登记回填到表单
5. 许可清单区维护允许与一律不允许两类许可，核查结论实时按项目汇总，并写清哪些登记按同一种许可对待
6. 任何一步不成立时，页面顶部给出说明，并尽量把问题标到对应的输入项上；删除项目与删除登记都会先确认

项目下面还挂着依赖登记时不允许删除项目，页面上会写清还剩多少条、例如哪几条，先处理这些登记再删。

## 许可清单与核查

- 许可清单分两类：允许使用的许可、一律不允许使用的许可，各自可以随时加入与移除
- 同一份许可只能待在其中一类清单里：已经在允许清单里的，要先移除才能加进不允许清单，反过来也一样；写法归一后相同的许可不允许在同一类清单里重复登记
- 核查时每条登记的许可先归一，再对照两类清单：落在允许清单里的通过、落在不允许清单里的不通过、两边都没写到的按待确认列出；没填许可的登记同样按待确认处理，不会悄悄放过，也不算作通过
- 结论按项目汇总：每个项目各有多少条通过、不通过、待确认，三个数字加起来就是这个项目的登记条数
- 许可写法的大小写、首尾空白、空格与短横线的差异都不算区别，常见别名（例如 `GPLv3` 与 `GPL-3.0`）也会归到同一个本名；统一处理意见里会写清哪些写法、哪些登记按同一种许可对待

## 登记口径

- 依赖名称以小写字母或数字开头，可以用小写字母、数字、点、下划线、短横线与斜线，最长 60 个字符；带范围的写法例如 `@types/node` 也按这个口径登记
- 版本写成三段数字，例如 `2.7.18`，需要时可以带一段预发布后缀，例如 `5.0.10-rc.1`
- 许可按实际名称填写，例如 `MIT`、`Apache-2.0`、`BSD-3-Clause`；还没确认的可以留空，列表里会显示成未填
- 责任人可以留空，表示这条依赖还没有定人
- 状态只有三种：在用、待升、已弃用
- 备注用来写为什么用这个版本、有没有待办，最长 200 个字符
- 同一个项目下不允许出现同名的依赖，同一个依赖在不同项目里各登记各的、互不覆盖

## 数据存储

所有数据写在 `data/db.json`，结构为 `{ "projects": [...], "deps": [...], "policy": { "allow": [...], "deny": [...] } }`，其中 `policy` 就是许可清单。落盘时先把内容写进同名临时文件，再用改名的方式替换正式文件，写入过程中断也不会把数据文件写坏；文件缺失或内容损坏时会自动回落到初始数据。

## 接口一览

| 方法   | 路径                          | 说明                                             |
| ------ | ----------------------------- | ------------------------------------------------ |
| GET    | /api/health                   | 服务状态检查                                     |
| GET    | /api/projects                 | 项目清单，附带每个项目的依赖条数                 |
| POST   | /api/projects                 | 新增项目                                         |
| PATCH  | /api/projects/:id             | 修改项目的名称、负责人或说明                     |
| DELETE | /api/projects/:id             | 删除项目                                         |
| GET    | /api/deps                     | 依赖登记列表，支持按项目、状态、许可筛选与关键词搜索 |
| POST   | /api/deps                     | 新增依赖登记                                     |
| GET    | /api/deps/:id                 | 查看单条依赖登记                                 |
| PATCH  | /api/deps/:id                 | 修改依赖登记                                     |
| DELETE | /api/deps/:id                 | 删除依赖登记                                     |
| GET    | /api/license-policy           | 许可清单：允许与不允许两类                       |
| POST   | /api/license-policy           | 往某一类清单里加入许可                           |
| DELETE | /api/license-policy/:list/:id | 从某一类清单里移除许可                           |
| GET    | /api/license-check            | 许可核查：按项目汇总、逐条结论与统一处理意见     |

接口出错时统一返回 `{ "error": { "code": "...", "message": "...", "field": "..." } }`，其中 `field` 用于让页面把问题标到具体的输入项上。
